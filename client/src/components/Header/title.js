import React from "react";
import styled from "styled-components";

const Title = () => {
  return <TitleText type="text">Build Phase</TitleText>;
};

const TitleText = styled.text``;

export default Title;
